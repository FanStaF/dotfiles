
################################
# alias
################################


alias hg='history | grep -i'

alias changephp='sudo update-alternatives --config php'

alias vim='/usr/local/bin/nvim'
# php aliases
alias ide-helper='php artisan ide-helper:generate && php artisan ide-helper:meta && php artisan ide-helper:eloquent'
alias art='php artisan'
alias serve='php artisan serve'
alias migrate='php artisan migrate --force'
alias tinker='php artisan tinker'
alias pt='phpunit --colors --bootstrap'
alias pint='php ./vendor/bin/pint --dirty'
alias llog='batcat storage/logs/laravel-$(date -u +%Y-%m-%d).log'
alias stan='~/code/wowbrands/r20-digital/vendor/bin/phpstan'

# Git aliases
alias G='git'
alias GS='git status'
alias GF='git fetch'
alias GC='git commit'
alias GL='git log --oneline'

alias mailhog='docker run -p 8025:8025 -p 1025:1025 mailhog/mailhog'
alias kali='docker run --rm --net=host -it --shm-size=512m -p 6901:6901 -e VNC_PW=password kasmweb/kali-rolling-desktop:1.16.0'
alias msf='docker run --rm --net=host -ti metasploitframework/metasploit-framework'

# enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    alias dir='dir --color=auto'
    alias vdir='vdir --color=auto'
    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi
